# OpenWRT Usefull Skills

# 1.modify the system config in img file

# 2.pack the user's ipk package into img file 

1.pack the ipk package(files)

2.install the package(uci-defaults)

3.startup running(rc.local)
